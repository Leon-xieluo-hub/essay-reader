# 文献阅读器（Essay Reader）开发 Prompt

> 用法：把下面「=== 主 Prompt 开始 ===」到「=== 主 Prompt 结束 ===」之间的全部内容，作为一个整体交给 AI 编码助手（或作为项目 README / 需求基线）。若后续要拆任务，可直接使用文末的「分阶段子 Prompt」。
>
> 本版核心特性：**双通道（本地模型 / 云端模型）+ 场景分工 + 离线优先**。用户可在同一界面内切换「免费离线但语感略弱的本地模型」与「按 token 计费但更准的云端模型」，两种模式共用同一套术语表与段落缓存，切换不丢工作；除首次下载模型权重外，软件运行期不依赖任何网络。

---

=== 主 Prompt 开始 ===

## 一、你的角色

你是一位资深全栈工程师 + 文档解析（Document AI）方向的专家，同时具备产品设计审美。请你为我从零构建一个名为 **Essay Reader（文献阅读器）** 的产品：一个以「上传 PDF → 精准解析 → 高质量翻译 → 结构化要点总结 → 图文表完整呈现」为主链路的文献阅读工具。

请以「可运行的工程」为目标输出：完整的目录结构、关键文件代码、依赖清单、启动方式、环境变量说明，以及每个技术难点的具体解决方案。禁止只给概念性描述或伪代码骨架。

## 二、产品目标与用户场景

目标用户：需要大量阅读英文/外文文献的研究生、科研人员、工程师。

核心场景：
1. 用户上传一篇（或批量上传多篇）学术 PDF（含双栏排版、公式、图表、参考文献）。
2. 系统解析出正文结构（标题 / 作者 / 摘要 / 章节 / 段落 / 图 / 表 / 公式 / 参考文献）。
3. 用户可切换「原文 / 译文 / 双语对照（逐段对齐）」三种阅读模式。
4. 系统生成文献要点：研究问题、方法、创新点、实验设置、主要结论、局限、可复用的点。
5. 用户可以选中任意段落/句子追问（问答、术语解释、长难句拆解），并能一键导出笔记。
6. 用户可在**本地模型通道**与**云端模型通道**之间自由切换：日常批量阅读走本地（零 token、零费用、文献不出本机），少数需要精读或引用的重要文献切云端；两种通道共用术语表与已翻译缓存，切换后无需重翻，断网时自动降级为本地且相关云端入口置灰。

> 三档运行模式定位如下，请在整个设计中贯穿：
> - **本地通道（默认）**：免费离线、隐私最优，术语一致性与长难句需质量校验兜底 → 用于整篇翻译与日常阅读。
> - **云端通道（按需）**：质量更高、速度更快，按 token 计费 → 用于重要文献的精读、总结与问答。
> - **关闭 AI（纯阅读）**：只做解析与阅读，翻译/总结/问答入口置灰，全程零 token、零网络。

## 三、界面与视觉要求（硬性）

- 整体风格：**简洁、清晰、留白充足、低干扰**，接近「纸质书 + 便签」的阅读感。
- 主色调：
  - 背景：米白 `#FAF7F0`（主）/ `#F5F1E8`（侧栏或次级面）
  - 强调色：淡黄 `#F7E7A6` / `#EFD98A`（高亮、选中、当前阅读位置）
  - 文字：深灰褐 `#2E2A24`（正文）/ `#6B6459`（次要信息）
  - 分割线：`#E6DFD0`
  - 功能色仅用于状态提示：成功 `#7C9A6D`、警告 `#C89B54`、错误 `#B5645A`
- 排版：正文使用衬线字体（如 `Source Serif 4` / `Noto Serif SC`）以贴合文献阅读，UI 控件用无衬线（`Inter` / `Noto Sans SC`）；行高 ≥ 1.7；正文最大宽度约 68–76 字符，保证长文可读性。
- 布局：
  - 顶部：极简工具栏（文件名、阅读模式切换、翻译开关、设置）。
  - 左侧：可折叠的目录 / 缩略图 / 文献列表，默认窄栏，不遮挡阅读。
  - 中部：阅读区，双语模式采用「同段落左右或上下对齐」而非整页分栏，避免错位。
  - 右侧：要点面板 / 对话面板（按需唤出，可全屏收起）。
- 交互细节：所有加载过程要有骨架屏或进度反馈；翻译按段落流式返回；悬停段落时高亮原文与译文对应关系；不得使用刺眼纯白 `#FFFFFF` 作为大面积背景。
- 请给出完整的设计 token（CSS 变量 / Tailwind theme 配置），而不是零散的颜色写法。

## 四、必须重点解决的技术难点

请针对以下三类问题，各自给出「为什么难 → 方案选型对比 → 最终实现（含代码）」的完整分析。

### 4.1 PDF 难以解析

必须处理：
- 双栏/多栏排版、跨栏图表、脚注、页眉页脚、行号、参考文献区。
- 扫描版 PDF（无文本层）→ OCR 兜底。
- 公式（行内与行间）、数学符号、上下标；化学式与特殊字符。
- 表格（合并单元格、跨页表格）→ 必须识别为结构化表格而非乱序文本。
- 阅读顺序（reading order）还原，段落合并与断行连字符（`hyphen-\nation`）修复。
- 编码与字体映射错误（乱码、`(cid:xx)`、私用区字形）。
- 大文件（>100MB、>500 页）的性能与内存控制。

实现要求：
- 采用「双通道解析」：优先使用带版面分析的解析器（如 PyMuPDF + 规则版面重建，或 MinerU / Marker / Docling / pdfplumber / GROBID 之类），失败或质量分低时回退到 OCR 通道（PaddleOCR / RapidOCR / Tesseract，或视觉模型直读页面图像）。
- 定义 **解析质量自检**：文本覆盖率、乱码率、段落长度分布、阅读顺序置信度、表格还原率；低于阈值自动切换通道并在 UI 上提示用户。
- 输出统一的 **文档中间表示（Document IR）**：JSON 结构，含 `pages[] / blocks[]`，每个 block 有 `type(text|heading|figure|table|formula|reference|footnote)`、`bbox`、`page`、`order`、`level`、`text`、`latex`（公式）、`table_html`、`image_path`。
- 解析结果必须缓存（按文件哈希），支持重复打开不重解析。

### 4.2 翻译时容易出现差错

必须处理：
- 学术长难句、被动语态、术语一致性（同一术语全文统一译法）。
- 缩写、专有名词、模型名、数据集名、单位、公式变量 **不得翻译**。
- 引用标记 `[12]`、上标引用、图号 `Fig. 3`、表号 `Table 2`、章节号必须原样保留。
- 上下文缺失导致的指代错误（`it / this / the former`）。
- 幻觉与漏译（模型自行增删内容）。

实现要求：
- 采用 **上下文感知的分块翻译**：按段落/小节切分，块大小受 token 预算约束，切分点必须落在段落或句子边界；每块携带「文档级术语表 + 前后各 N 段上下文 + 论文标题与摘要」作为提示词上下文。
- **术语表（Glossary）机制**：首次解析时抽取术语（关键词/缩略语/命名实体），生成统一译法并持久化；翻译时强制注入；允许用户在 UI 中修改术语并触发局部重译。
- **结构化占位符保护**：翻译前把公式、引用、图号、URL、代码替换为不可翻译占位符 `<ph id=..>`，翻译后精确回填；回填失败则重试或降级为原文。
- **质量校验回路**：对每段译文做自动检查——长度比异常、数字/公式/引用数量是否与原文一致、是否包含占位符残留、是否出现目标语言以外的整句；不合格段落自动重译（带上失败原因），仍不合格则在 UI 上标记并允许人工修正。
- 翻译结果按段落缓存并支持「原文/译文」逐段对齐；用户手动编辑译文后不得被覆盖。
- 明确 **模型与密钥配置**：通过环境变量配置 LLM 提供商与模型（本地模型可选），API 失败要有重试 + 退避 + 并发限流；不得把密钥写进前端或仓库。
- 翻译实现必须**与通道解耦**：翻译逻辑只依赖统一的 Provider 接口，不得把某一家 API 或某个本地模型的调用细节写死在翻译服务里；这样同一条流水线（术语表 + 占位符保护 + 质量校验）在本地通道与云端通道上行为一致。**质量校验回路在本地通道下必须强制开启**（本地模型数字与引用出错率更高，这是主要兜底手段），云端通道可配置为轻量校验。

### 4.3 图片与表格难以呈现

必须处理：
- 图片位置错乱、被切成两半、跨页图、图与图注（caption）分离。
- 表格失去结构、公式化表格、三线表样式。
- 图注/表注需要跟随翻译，但图内文字不属于正文文本流。

实现要求：
- 图片：解析阶段用 bbox 从页面裁切原图（高 DPI 渲染，如 200–300 DPI），保留 caption 与其绑定关系；UI 中以「原位内联 + 点击放大（灯箱）」呈现；跨页图自动合并或标注「续下页」。
- 表格：优先还原为 HTML/结构化数据，用自定义表格组件渲染（支持横向滚动、固定表头、行列表头区分、脚注保留）；同时提供「查看原图」切换，以便用户核对；表格内容随正文一起翻译（表头与单元格分别翻译，保持行列对应）。
- 公式：能转 LaTeX 的转 LaTeX（KaTeX/MathJax 渲染），不能转的保留原区域图像；翻译时公式不译。
- 图片内文字（图表标签、坐标轴）：可选做 OCR/翻译，并以「悬停显示原文/译文对照」方式呈现，绝不覆盖原图。
- 所有图文资源本地化存储并做防盗链/路径安全处理。

## 五、双通道与场景分工（本地模型 / 云端模型 + 离线优先）

本节是产品的核心策略，请在架构、代码与 UI 三个层面同时落地。

### 5.1 通道能力矩阵（必须实现）

用户可**按文献**或**按功能**选择通道，而不是全局二选一。

| 功能 | 本地通道（默认） | 云端通道（按需） | 纯离线可用性 |
|---|---|---|---|
| 上传、解析、版面重建、扫描件 OCR | 本地引擎 | — | ✅ 完全离线 |
| 原文阅读、缩放、目录、搜索、进度 | — | — | ✅ 完全离线 |
| 图表裁切、表格还原、公式渲染 | — | — | ✅ 完全离线 |
| 段落对齐、高亮、笔记、导出 | — | — | ✅ 完全离线 |
| 术语抽取、翻译质量校验 | 本地模型 / 纯规则代码 | 云端模型 | ⚠️ 规则部分离线，模型部分需本地模型就位 |
| **翻译（整篇）** | ✅ 零 token、零费用、数据不出本机 | ✅ 更准更快、按 token 计费 | ⚠️ 需本地模型；未就位则该功能置灰 |
| **要点总结** | ✅ 免费离线 | ✅ 深度更好 | ⚠️ 同上 |
| **划词问答** | ✅ 免费离线，推理深度有限 | ✅ 更可靠、出处判断更准 | ⚠️ 同上 |
| 模型权重下载 | 首次一次性联网 | — | ❌ 仅此一次需要网络 |

**明确写出**：除首次下载模型权重外，软件运行期不得依赖任何外部网络；不做遥测、不上报使用数据。

### 5.2 Provider 抽象层（后端硬性要求）

- 定义统一的 LLM 接口，至少包含：`chat(messages, schema?)`、`stream_chat(...)`、`embed?`、`health()`、`capabilities()`（上下文上限、是否支持结构化输出、是否支持流式、预估吞吐）。
- 至少实现两个适配器：
  - `LocalProvider`：对接本地推理引擎（推荐 Ollama：`http://127.0.0.1:11434`，或 llama.cpp / LM Studio 的 OpenAI 兼容端点）；由后端进程负责启动与健康检查，用户不需要手敲命令行。
  - `CloudProvider`：对接 OpenAI 兼容 API（DeepSeek / OpenAI / 自建中转皆可），密钥只从后端环境变量或本地加密配置读取。
- 配置项（`.env.example` 必须给出）：`LLM_PROVIDER=local|cloud|off`、本地模型名与端点、云端 base_url / model / api_key、并发度、超时与重试次数、单篇 token 预算上限。
- 失败与降级规则：
  - 本地引擎未安装或模型未就位 → 云端入口可用、本地入口给出「一键安装/下载模型」引导，而不是报错。
  - 断网或云端密钥缺失 → 云端入口置灰并说明原因，本地通道照常工作。
  - 云端调用失败 → 有限次重试后**询问用户**是否降级到本地通道（不得静默降级，避免用户在不知情下拿到质量更低的译文）。
  - 单篇 token 预算耗尽 → 停止并提示，已翻译段落保留。

### 5.3 本地模型部署由软件负责（不得把门槛丢给用户）

- **首次启动引导**：检测本地推理引擎是否存在 → 不存在则引导安装（或随包分发）→ 检测所选模型权重是否存在 → 不存在则**在应用内提示「正在下载翻译模型（约 4~5 GB），仅需一次」**并显示进度、支持暂停与断点续传、失败可重试。
- 安装包保持轻量，模型权重按需下载，不硬塞进安装包。
- **离线安装包支持**：为无外网环境（实验室内网、涉密机器）提供「模型离线导入」入口——从 U 盘或内网路径导入模型文件包，跳过联网下载。
- 支持在设置中查看与管理本地模型：已安装列表、体积、量化等级、显存占用估算、删除、切换。
- 模型默认值建议按目标机器给出推荐档位（例如 8 GB 显存推荐 8B 级 Q5/Q6 量化；宁可小模型高精度量化，也不要大模型激进量化——量化等级对术语与数字保真的伤害大于参数规模差异）。

### 5.4 显存与并发约束（本地通道必须遵守）

- **串行调度**：本地通道下，OCR 阶段与 LLM 推理阶段不得同时运行（两者争抢显存会 OOM），必须在任务队列中串行编排。
- **并发降级**：本地通道翻译并发限制为 1~2（可配置），并相应调小分块 token 上限、上下文窗口与批量段落数；云端通道可用更高并发。
- 给出显存预算表与自动降级策略：显存不足时按「减小上下文 → 减小分块 → 降低并发 → 提示切换更小模型」的顺序自动降级，并记录日志。
- 不要把 embedding / rerank 模型塞进同一块显存；问答检索优先用 BM25 等纯 CPU 方案，或显式告知用户需额外显存。
- 本地通道需要**取消与暂停**能力：一次整篇翻译可能持续十几分钟，用户必须能随时中断并从已完成段落继续。

### 5.5 界面与信息架构（双通道必须可见、可解释）

- 顶栏常驻**通道指示器**：显示当前用的是「本地·模型名」还是「云端·模型名」，点击即可切换；文献列表与阅读区对每篇文献显示其翻译所用通道的标签。
- 切换通道前的**影响说明**：预计耗时、是否产生费用（云端显示「本篇预计消耗约 X token / ¥Y」，本地显示「免费，预计约 N 分钟」）；若该文献已有另一通道的译文，询问是「复用现有译文」还是「用新通道重译（仅针对被标记的段落）」。
- **质量提示（必须诚实，不得让用户误以为两种模式结果相同）**：例如「本地模型 · 免费离线，长难句与专业术语建议人工复核」/「云端模型 · 更准确，按 token 计费」。被质量校验标记为可疑、且自动重译仍未通过的段落，要在正文旁有可见标记并提供「用云端通道重译此段」的一键入口。
- 离线状态下顶部给出轻量状态条：「当前离线 · 使用本地模型」，云端相关按钮置灰并说明原因。
- 费用与用量可见：设置中提供本地/云端使用统计——云端已消耗 token 与估算费用、本地已处理段落数与耗时，可清零。
- 三档模式在设置中可直接切换：`本地优先（默认）` / `云端优先` / `关闭 AI（纯阅读）`。

## 六、功能清单（必须实现）

1. PDF 上传：拖拽/选择、多文件、进度、失败重试、文件管理与删除。
2. 解析流水线：状态可见（排队/解析中/翻译中/完成/失败），可取消。
3. 阅读器：分页与连续滚动、缩放、目录跳转、页内搜索、段落定位、阅读进度记忆。
4. 双语对照：原文/译文/双语三模式，段落级对齐高亮联动。
5. 要点总结：自动生成结构化摘要（研究问题 / 方法 / 数据与实验 / 主要结论 / 创新点 / 局限 / 关键数据），支持导出 Markdown。
6. 划词与问答：选中文本 → 翻译 / 解释 / 追问，基于该文献内容作答并给出处（章节 + 页码），无依据时明确说明。
7. 笔记与标注：黄色高亮、批注、导出（Markdown / PDF 摘录）。
8. 术语表管理：查看、修改、应用到全文。
9. 设置：语言对、翻译风格（直译/学术通顺）、**通道选择与本地模型管理（安装/下载/导入/切换/删除、显存档位）**、云端 API 配置与用量统计、并发与超时、单篇 token 预算、主题色深浅、离线安装包导入。
10. 数据本地优先：文献、译文、笔记存储在本地（或用户自选后端），提供导入/导出与清空。

## 七、技术栈要求

- 前端：React + TypeScript + Vite（或 Next.js App Router），状态管理用轻量方案（Zustand / Jotai），样式用 Tailwind CSS + 设计 token，公式用 KaTeX，PDF 展示用 `pdf.js`（渲染原页面与缩放），虚拟列表处理长文档。
- 后端：Python（FastAPI）为主，负责解析、OCR、翻译编排、缓存与文件存储；解析库优先 PyMuPDF、pdfplumber，可选集成 MinerU / Docling / Marker，OCR 用 PaddleOCR 或 RapidOCR。
- 存储：本地文件系统 + SQLite（元数据、段落、译文、术语、笔记）；缓存目录结构清晰。
- 本地推理：Ollama（首选，跨平台、自带模型拉取与显存管理）或 llama.cpp / LM Studio 的 OpenAI 兼容端点；后端负责进程启动、健康检查、模型拉取与显存策略；保留切换到 vLLM 等高性能引擎的扩展点。
- 任务队列：异步任务 + 进度上报（可用 asyncio + 后台任务，或 Celery/RQ；单人本地部署优先轻量方案）；队列需支持**串行编排**（OCR 与本地推理不同时占显存）与**任务取消/暂停/续跑**。
- 部署：一条命令本地启动（`docker compose up` 或 `pnpm dev` + `uvicorn`），提供 `.env.example`；不依赖任何云端专有服务，LLM 可替换；**运行期零网络依赖，仅首次模型下载需要联网，并提供离线模型包导入路径**。
- 工程质量：模块边界清晰、类型完整、错误处理明确、关键路径有日志；为解析 IR、占位符回填、质量校验、**Provider 抽象层（本地/云端行为一致性）** 这四个核心模块写单元测试。

## 八、交付物与输出格式

请按以下顺序输出，不要跳步：

1. **技术方案总览**：架构图（文本/mermaid）、数据流（上传 → 解析 → IR → 翻译 → 渲染 → 总结）、目录结构树，并标出**本地通道与云端通道的分界点**（哪一层负责 Provider 选择、哪一层负责串行调度）。
2. **数据模型**：Document IR 的完整 JSON Schema、数据库表结构、缓存键设计；缓存键必须体现「段落 × 通道 × 模型」维度，以支持双通道译文共存与按段复用。
3. **难点专项方案**：分别针对 4.1 / 4.2 / 4.3 给出选型对比与最终实现，附关键代码。
4. **双通道专项方案**：Provider 接口与两个适配器代码、本地引擎启动与模型自动下载/离线导入流程、串行调度与显存降级策略、通道切换与按段复用逻辑、费用与用量统计实现。
5. **后端代码**：解析服务、翻译服务（含占位符保护、术语表、质量校验、重试）、总结服务、API 路由。
6. **前端代码**：设计 token、布局组件、阅读器、双语对齐组件、表格与图片组件、要点面板、问答面板、通道切换与模型管理界面、质量提示与离线状态条。
7. **提示词模板**：翻译、总结、问答、术语抽取四类提示词，写成可维护的模板文件（含变量占位与输出格式约束），翻译与总结要求结构化输出（JSON）并给出解析与容错策略。**本地与云端通道可使用不同模板**，并说明各自的取舍：本地小模型需要更严格的格式约束、更小的块与更强的术语强制；云端模型可放宽约束、增大上下文。
8. **测试与验证**：单元测试、3 篇典型 PDF（单栏 / 双栏带图表 / 扫描件）的端到端验证步骤与预期结果，并额外覆盖：本地/云端通道切换后术语与译文复用、断网场景、显存受限时的降级路径。
9. **运行说明**：环境要求、安装、首次模型下载、纯离线部署（离线模型包导入）、启动、常见问题、显存不足时的调参建议。
10. **风险与后续路线**：当前方案已知限制（含本地模型在长难句与语感上的已知短板）、可优化项、下一个迭代计划。

## 九、约束与验收标准

- 视觉：整体必须呈现米白 + 淡黄的低干扰阅读氛围；不允许出现大面积纯白、深色模式为可选项（默认跟随系统）。
- 准确性：译文不得丢失或新增事实；数字、引用、公式、图号必须与原文一致；引用标记与缩写保持原文形态。
- 完整性：图表必须可见可读，表格保持行列结构，公式正确渲染。
- **离线可用性**：首次模型下载完成后，断网状态下必须能完成「上传 → 解析（含扫描件 OCR）→ 阅读 → 本地翻译 → 总结 → 问答 → 笔记导出」全链路，全流程无任何外部网络请求依赖；不采集、不上报任何用户数据。
- **双通道一致性**：切换通道不得丢失已翻译段落、术语表、笔记与阅读进度；缓存必须支持同一段落的多通道译文并存并按需复用；云端不可用时给出明确原因而非静默失败，且**不得静默降级**到本地通道。
- **成本可见与可控**：云端通道每次翻译/总结前给出预估 token 与费用，使用后可查看实际消耗；支持单篇 token 预算上限，超限即停止并保留已有结果；本地通道明确标注「免费离线」。
- **诚实的能力提示**：界面必须区分呈现两种通道的质量定位（本地：免费离线、长难句与术语建议复核；云端：更准确、按量计费），不得让用户误以为两者结果相同。
- 性能：100 页 PDF 从上传到可读 ≤ 60 秒（不含翻译）；云端通道翻译按段落流式输出、首段译文 ≤ 5 秒出现；**本地通道需给出明确进度与剩余时间预估**（8 GB 显存、8B 级量化的参考基线：单篇 20 页约 10~25 分钟，并在文档中说明该基线随硬件波动）；界面在长文档滚动时保持流畅（虚拟化）。
- 资源约束：显存不足时按既定策略自动降级且不崩溃；OCR 与本地推理不同时占用显存；整篇翻译可随时暂停/取消并从已完成段落续跑。
- 可用性：任何失败都要有明确提示与「重试 / 更换解析通道 / 更换模型通道 / 查看原始页面」的出口。
- 可解释：总结与问答的每个结论尽量给出处（章节 / 页码），无依据时说明「文中未提及」。

如果某个需求存在多种合理解法，请先给出 2–3 个方案与取舍建议，再给出你推荐的实现；如果你认为我的需求中有不合理或可优化之处，请直接指出并给出替代方案。

=== 主 Prompt 结束 ===

---

## 附：可选的简化版 Prompt（用于快速起项目）

用 React + TypeScript + Tailwind 做前端、FastAPI + PyMuPDF 做后端，构建一个「文献阅读器」：米白（#FAF7F0）背景 + 淡黄（#F7E7A6）高亮的简洁阅读界面。用户上传学术 PDF，后端解析为结构化文档 IR（段落/标题/图/表/公式，保留 bbox 与阅读顺序，支持双栏、扫描件 OCR 兜底），前端用 pdf.js + 虚拟列表渲染；翻译走「段落对齐 + 术语表 + 占位符保护（公式/引用/图号）+ 质量校验重译」的上下文分块翻译，支持原文/译文/双语三种模式；翻译与总结走**双通道架构**——统一的 LLM Provider 接口 + 本地推理适配器（Ollama，软件内引导下载模型，运行期完全离线、零 token）与云端 API 适配器（OpenAI 兼容，按 token 计费），用户可按文献在两者间切换且共用术语表与段落缓存；图片按 bbox 裁切内联呈现并绑定图注，表格还原为 HTML 结构并可与原图对照；自动生成结构化要点总结（研究问题/方法/结论/创新点/局限）并支持导出。请输出完整目录结构、数据模型、关键代码与运行说明。

## 附：分阶段子 Prompt

- **阶段 1｜骨架与视觉**：搭好前后端骨架与设计 token，实现上传 + 用 pdf.js 原样渲染页面 + 米白淡黄主题，跑通端到端。
- **阶段 2｜解析与 IR**：实现 PyMuPDF 解析、版面重建、阅读顺序、图表裁切、扫描件 OCR 兜底、解析质量评分与 IR 持久化。
- **阶段 3｜翻译流水线**：实现分块翻译、术语表、占位符保护与回填、质量校验与重译、段落级缓存与流式输出；同时落地 LLM Provider 抽象层与本地推理适配器（Ollama 启动与模型自动下载），使本地通道端到端跑通。
- **阶段 4｜阅读体验**：双语对齐联动、目录、搜索、高亮笔记、阅读进度、图表灯箱与表格组件。
- **阶段 5｜总结与问答**：结构化要点总结 + 带出处的问答 + 导出（同样走双通道）。
- **阶段 6｜双通道与离线打磨**：云端适配器与通道切换 UX、按段复用与费用/用量统计、质量提示与离线状态条、串行调度与显存降级、离线模型包导入。
- **阶段 7｜打磨与发布**：性能与虚拟化、错误与重试 UX、测试用例（单栏/双栏/扫描件三篇样本 + 断网与显存受限场景）、打包与部署。
