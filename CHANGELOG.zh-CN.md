[English](CHANGELOG.md) | **简体中文**

# 更新日志

记录 Essay Reader 的重要变更，最新在前。每个版本在本仓库都有对应 tag，tag 页面附带同样的说明。

## 0.2.1 — 2026-09-21

0.2.0 之后、在实际运行的应用里复测时发现的问题。

### 修复

- **Elsevier 类 PDF 的加粗/斜体识别不到。** 这类 PDF 用的是子集字体，样式只体现在字体名后缀（`AdvTT28000ce1.B` 表示粗体、`AdvTT94c8263f.I` 表示斜体），
  PyMuPDF 的 bold / italic 标志位并没有置位，于是整篇论文一个样式都没有。现在同时识别字体名后缀：
  Comprehensive review 由 0 个带样式块 → **96 个**。
- **前端重建后可能被浏览器缓存挡住。** SPA 外壳改为 `no-cache, must-revalidate`，带内容哈希的包改为 `immutable`，
  这样界面更新在普通刷新下必然生效（此前一次界面改动就是这样「没做出来」的）。
- **文献库里保存的是上传时的解析结果。** 解析结果按文件哈希缓存，所以在解析器修复之前上传的论文仍然显示旧分段，
  而且重新上传同一份文件只会返回缓存副本。现在库中 9 篇已全部用新解析器重跑；
  `GET /api/documents/{id}` 也会返回 `emphasis`，一眼就能看出当前是哪个版本的解析结果。

### 变更

- 界面语言开关更好找：顶栏控件显示 `中 / EN`（不再是单个字形），设置 → 阅读与外观 里也有同一开关，
  并注明「默认跟随系统语言」。

## 0.2.0 — 2026-09-21

这一轮由 **9 篇真实论文**（8~21 页，单栏/双栏，含公式密集型）驱动。上一轮的分段修复只在单篇论文上验证过；
在这批论文上实测，它并不普适。

### 修复

- **被排版切断的段落现在能正确接回。** 判据从「风格」换成「版心」：段落最后一行**顶到栏右边界**说明被版心截断、必然还有下文。
  原来的「首行缩进」判据在 MDPI / Frontiers / Elsevier 那种块状分段排版里只对 1%~13% 的段落生效。
  参考论文上接回的段落由 4 段增至 **16 段**，且不再有任何页末断句（断裂 0 / 页末残句 0，段落中位长 389 → **473** 字符）。
- **表格造成的文本缺失。** 表格区域只能吞掉它自己还原出来的内容：会比对「被剔除行的字符数」与「重建单元格字符数」，
  覆盖不足 80% 就把原始文本留在正文里并写入告警。实测覆盖率：Comprehensive review 0.963 → **0.997**、
  *Using color and 3D geometry* 0.874 → **0.964**、Field-grown tomato 0.949 → **0.971**、fpls 0.943 → **0.960**、
  Leaf Area 0.985 → **0.993**。
- **公式裁图造成的文本缺失。** 正文被排除出裁图矩形、且正文行永不并入图片；同时方程依然成图，
  包含 `z` / `ph` / `V =` 这类「只有作为图片才看得懂」的纯字形公式。
- **表 1 少最后一行**（17 行 → **18 行**）。左格折行的行会把右格缩写放在两条基线之间，
  基线分组看不到成对的行，整行因此被丢到表格外面；现在这类折行会被显式吸收。
- **只提到算式的段落**（`…R² = 0.72…`）不再被改判为公式 —— 字体不变、留在正文流里、照常参与翻译。
- **切换阅读模式不再丢失进度。** 双语 / 译文 / 原文会改变每个块的高度，浏览器钳制滚动位置后看起来像跳回开头；
  现在会把「视口顶部所在的段落」重新钉回顶部。
- **目录不再沉到整份文献库之后**，改为紧跟在当前文献卡片下方。
- **跑头改为按形态识别**（而非仅按 y 坐标），页面顶部偏高的正文不再被误判成跑头。

### 新增

- **行内加粗 / 斜体。** 按 span 提取为 `Block.emphasis`（记录块文本中的字符偏移），渲染为 `<strong>` / `<em>`，
  并与搜索高亮叠加。
- **界面语言跟随系统**（中文系统 → 简体中文，其余 → 英文），顶栏 `中 / EN` 可手动切换并记住选择。
  共 313 条文案，位于 `frontend/src/i18n/`，无缺失键。
- `blocks.emphasis` 字段（增量迁移，已有文献库不受影响）。

## 0.1.0 — 2026-09-19

首次公开发布。

### 新增

- PDF → 文档 IR：栏与元信息侧栏识别、阅读顺序、无框线表格还原、200 DPI 图表与公式裁切、目录净化、解析自检
  （覆盖率、乱码率、段落/图/表数量）。
- 扫描件离线 OCR 兜底（RapidOCR / ONNX，CPU），识别结果并入同一套版面逻辑。
- 双通道翻译（本地 Ollama / OpenAI 兼容云端），共用一套术语表与段落级缓存；
  引用、DOI、URL、日期、数字走占位符保护，逐段质量校验，可疑段落可一键云端重译。
- 结构化要点 + 带出处的问答（引用 block id）。
- FastAPI 后端单端口托管构建好的 React/Vite 前端；文献、译文、笔记、图表全部留在本机 SQLite，无遥测。
- 回归测试：解析器（双栏 / 单栏 / 扫描件 / 真实 17 页论文）与服务层冒烟测试，全部只写临时目录。
- 双语文档（英文 README + `README.zh-CN.md`）、界面截图，以及会自动构建、清理端口并打开浏览器的 Windows 启动器。
