# 文献阅读器 · 改进 Prompt（第二轮）

> 用法：把下面「=== 主 Prompt 开始 ===」到「=== 主 Prompt 结束 ===」之间的内容交给 AI 编码助手。
> 本文件的前半部分是**根因分析**（已由真实文献复现并定位），后半部分是据此生成的改进 Prompt。
> 基线是当前仓库里的 Essay Reader（`backend/` + `frontend/`），请在此基础上增量修改，不要重写架构。

---

## 一、问题与根因分析（已复现）

复现样本：`3DPhenoMVS.pdf`（17 页，MDPI 单栏，带跑头 "N of 17"、编号章节、跨行大标题）。
诊断方法：直接调用 `app.parsing.parse_pdf` 并与 `page.get_text()` 逐页对比。

### 问题 1：第 5 页（及类似页面）分段碎裂、句子被拆成小块

**现象**：第 5 页被切成 37 个块，正文一句话占一个块，读起来断裂。

**根因（两个叠加的缺陷，均与"段落合并"无关）**：

1. **行级独立判定过宽**。`_line_is_standalone()` 会因 `_SECTION_NUM` 这类「以数字开头」的规则把正文行判定为"必须独立成段"的行，于是合并时在每一行前都 flush 一次。
   例如正文行 `"100 images were taken for one fruit. First, …"` 被当成编号小节标题（`100` 被当作节号），从而把它和前后行都切开。
2. **分类规则把正文误判为标题**。旧规则 `^\s*\d+`（任意位数）与 `sentences <= 1` 组合，会让
   `"100 images were taken…"`、`"8 positions where…"`、`"0.51 cm, 0.42 cm, 0.98 cm…"` 全部变成 heading。

**已修复**（本轮）：新增 `_looks_like_section_heading()` 统一判定，并要求
节号 ≤ 2 位且最多 3 级、`rest[0]` 必须为字母、拒绝 `0.x` 测量值、拒绝"数字+逗号+数字"的测量串、
拒绝 `29 June 2022)` 这类元数据；`_line_is_standalone()` 同时收紧（只对页码/图注/真标题/冒号结尾生效）。
结果：第 5 页 37 → 8 块，段落中位长度 99 字符，跨块句子完整，全篇文本覆盖率 100%。

### 问题 2：目录混入页码与正文碎片

**现象**：目录里出现 `2 of 17`、`3 of 17` … 以及 `0.72 to 0.97. In addition, …` 这类正文。

**根因**：
1. **跑头未被识别为页眉**。`_mark_headers_footers()` 只按"短文本 + 位于页面顶部/底部"判断，而 MDPI 的跑头在 `y≈57`（页高 842 的 6.8%），刚好越过 6% 阈值；同时它字号略大于正文，于是被 `_matches_heading_style` 判成 heading。
2. **测量数值被当成节号**。`0.72 to 0.97` 的数字前缀 `0.72` 通过了"编号小节"规则。
3. **跨行大标题被拆成多个 L1**。论文标题分 3 行，每行都各自成为一个 heading。

**已修复**（本轮）：
- 新增 `_PAGE_NUMBER`（`N of M`、`Page N of M`、`N/M`）与 `_FOLIO_ONLY`，在 `_mark_headers_footers()` 与标题判定中优先否决，并统一转成 `header`/`footer` 类型；
- 测量值/测量串/日期元数据否决（见问题 1）；
- `_refine()` 中加入**跨行标题合并**：把第 1 页顶部连续、间距小于 ~2.5 行高的 heading 合并为一个 L1，续行标 `title-continuation`；
- 用 span 颜色识别"浅色文字块"（期刊页眉色带上的白字，如 `agronomy`），标 `faint-text` 并排除出目录。

结果：`N of 17` 类目录项 16 → 0，测量值目录项 → 0，标题合并为一个 L1，目录只剩 `1./2./2.1./…` 等真实章节（共 25 项）。

### 问题 3：切换模型通道的下拉面板被正文盖住

**根因**：顶栏使用 `backdrop-blur`（`backdrop-filter`），会创建**新的层叠上下文**；面板即使 `z-index: 40` 也只能在顶栏内部比较层级，而阅读区在顶栏之后绘制，于是盖住面板。这是层叠上下文问题，不是 z-index 数值不够。

**已修复**（本轮）：新增 `FloatingPanel`，用 `createPortal` 渲染到 `document.body`，以 `position: fixed` + 实测锚点坐标定位，并处理窗口 resize/scroll 与外部点击、Esc 关闭。通道菜单与导出菜单都改用它。

### 问题 4：通道不可用时只报错，没有可执行的修复路径

**现象**：本地未装 Ollama 或云端未配 Key 时，"翻译全文"只是禁用或报错，用户不知道下一步做什么。

**已修复**（本轮）：
- 后端新增 `GET /api/setup/guide`：返回本地/云端的可用状态、`needs`（`install_runtime` / `pull_model` / `ready`）、是否检测到 Ollama 可执行文件、已安装模型、官方链接与命令；
- 后端新增 `POST /api/setup/download-ollama`：以 SSE 流式下载官方安装包并落盘到 `backend/data/downloads/`，返回进度与最终路径（只下载，不静默安装）；
- 前端新增 `SetupGuidePanel`：按依赖顺序给出 4 步（装 Ollama → 启动服务 → 一键下载模型 → 切换通道），每步显示状态徽标、一键按钮、官方链接与等价命令行；云端则给 Base URL / API Key 表单 + 获取 Key 的链接；
- 顶栏通道菜单对不可用通道显示「查看启用步骤」，主按钮在通道不可用时变为「启用通道」并直接打开引导。

### 问题 5：分段仍有两处错误（一个并段、一个分页拆段）

**现象**（用户在真实论文上复现）：
1. 两个互相独立的段落被合并进同一个文本块；
2. 原文中同一段落在 PDF 里跨页后，被识别成两块并分别落在两页上。

**根因**：
1. **并段**：段落合并只用了"行距阈值"这一条判据。当上一段最后一行与本段第一行行距正常（没有额外段间距）时，新段落的**首行缩进**就成了唯一的视觉线索，而这个线索没有被使用，于是两段被并成一块。反过来说，只有行距判据时，行距稍大的标题/公式行又会被误当成新段，因此不能简单调阈值。
2. **分页拆段**：分块完全以页为单位进行，页末的半个句子与下一页开头的下半句各自成块；没有任何"跨页接续"环节，页末句子的残缺状态也没有被检测。

**已修复**：
- `_group_paragraphs()` 新增 `_column_margin()` 估计栏内正文左边距，并加入缩进规则：`x0` 落在 `margin+8 … margin+30`、行宽 ≥ 上一行 80%、且上一行未缩进时，判定为**新段起点**（首行缩进的"悬挂"特征），与行距判据并列使用；
- `_refine()` 最前面新增 `_join_page_split_paragraphs()`：若本页最后一个文本块以 `_ends_mid_sentence()` 判为残句，则从下一页开头寻找接续块（`y0 ≥ 80` 排除页眉、行宽 ≥ 200、`x0 - margin ≤ 8` 即未缩进），把文本/图注拼回同一块并按 `order` 重新编号；
- 回归断言（`test_parser.py`）：页末残句数 ≤ 1、三处跨页句子合回同一块、跨页接续不产生重复文本、段落数与块数落在合理区间。

结果（真实 17 页论文）：块数 195 → **181**，段落 **110**，页末残句 **0**，重复文本 **0**。

### 问题 6：占位符保护会切碎技术标记，编号列表也会被送去翻译

**现象**（AI 在执行问题 5 的验证时，从逐段质检里发现）：
1. 标题 `3DPhenoMVS: A Low-Cost 3D Tomato Phenotyping…` 译成「…低成本3D番茄表型分析流程 **3**」——句尾多出一个孤立的 `3`；
2. 日期被译坏：`1 February 2020` → 「1 年 2020 月」，`29 June 2022` → 「29年2022月」；
3. 纯编号块 `5. 6. 7. 8. 9. 10.` 被翻译成「5。6。7。8。9。10。」；
4. 公式块里只有一条 Google Drive 链接，同样被送去翻译（公式本应是图片、不参与翻译）；
5. 质检误报：9 个受保护标记的段落被判「数字疑似丢失（原文 9，译文 8）」——把 `<ph id="7"/>` 里的占位符编号当成了论文里的数字。

**根因**：
1. 数字规则 `(?<![\w.])\d+…` 只有**前视**边界，没有**后视**边界，于是 `3D` 里的 `3` 被单独遮成 `<ph id="0"/>D`。模型看到「占位符 + D」，重排后只回填了占位符，`3` 就落到了句尾；
2. 日期没有任何保护规则，模型把 `Month Year` 当普通词序处理；
3. `select_blocks()` 只看类型和长度，不看「遮完还剩什么」，编号列表、纯链接行都会进入翻译；
4. `TRANSLATABLE` 里包含 `formula`，与 README 中「公式不参与翻译」的说明不一致；
5. `validate()` 在**遮罩后的文本**上数数字，而遮罩后的文本里恰好全是占位符编号。

**已修复**：
- `num` 规则补上尾部负向断言 `(?![\w])`，另加一条「数字+大写字母短标记」规则（`3D` / `2D` / `5G`）整体保护；`3DPhenoMVS` 这类词内数字保持原样不被拆开；
- 新增 4 条 `date` 规则（`1 February 2020`、`February 1, 2020`、`2020-06-29`、`2020 年 6 月`），整体保护、原样回填，并纳入 `validate()` 的数量校验；
- `select_blocks()` 增加「遮掉占位符后必须还剩字母或汉字」的判据，纯编号/纯链接块直接跳过；
- `TRANSLATABLE` 移除 `formula`（公式继续以裁图呈现，其文本层本就是乱码）；
- `validate()` 的数字化校验先剔除占位符再计数（占位符编号不是论文里的数字）。

结果（同一篇 17 页论文，云端通道重跑）：待翻译块 123 → **109**，`suspect` 8 → **1**（仅剩参考文献列表：模型对整段引文直接原样返回，属已知取舍，见 README）。标题不再出现孤立数字，日期原样保留，token 用量下降约 18%。

### 问题 7：分段规则不普适（缩进假设失效）

前两轮的修复只在**一篇**论文上验证。这一轮用 **9 篇真实论文**（8~21 页，单栏/双栏，含公式密集型）做基线测量，
证明「分段与文本缺失」不是单篇特例：

| 论文 | 块数 | 段落数 | 覆盖率 | 续接断裂 | 页末残句 | 中位段长 |
| --- | --- | --- | --- | --- | --- | --- |
| 3DPhenoMVS | 181 | 110 | 0.985 | 0 | 0 | 389 |
| applsci-15-08406 | 195 | 114 | 0.962 | 5 | 0 | 258 |
| Comprehensive review | 279 | 212 | 0.963 | **29** | 5 | 360 |
| Field-grown tomato | 408 | 309 | **0.949** | 18 | 2 | 100 |
| fpls-14-1045545 | 392 | 277 | **0.943** | 22 | 2 | 94 |
| Leaf Area Estimation | 182 | 118 | 0.985 | 5 | 1 | 186 |
| plants-14-02080 | 205 | 109 | 0.964 | 9 | 2 | 211 |
| sustainability-17-10120 | 272 | 176 | 0.994 | 4 | 2 | 289 |
| Using color and 3D geometry | 214 | 124 | **0.874** | **41** | 4 | 137 |

**根因**：上一轮用「首行缩进」判断新段。实测这 9 篇的首行缩进比例只有 **1%~13%** —— MDPI / Frontiers / Elsevier 用的是
**块状分段**（段间留白、不缩进），缩进信号几乎失效，跨页续接因此判不出来。

**修复**：改用**版心信号**——段落最后一行是否**顶到栏右边界**：被版心截断的行必然要继续（这正是「前一行文字是否接续」的排版学表述），
与是否缩进无关、对两种排版风格都成立。合并循环另加否决条件：下一个块若是章节标题、行内标题（`Stem length:`）、图注，
或中间夹着图/表/公式块，则不合并；只有「顶满版心 + 下一页以大写字母开头」同时成立时，才允许跨过浮动图表合并。
`_refine()` 的次序也调整为「先标记跑头等非正文块，再做续接合并」（页首跑头此前会挡住候选）。

### 问题 8：其他论文文本缺失严重

**根因**（三个独立来源）：
1. **表格区域剥离后没有回填**：重建出的行数不足时，被剥掉的行直接消失（UsingColor 丢 60 行、Comprehensive 丢 32 行）；
2. **公式区域吞掉正文**：以 `(n)` 为锚点时把「编号左侧的所有行」都算进公式，其中包含正文行（Field-grown 丢 18 行）；
3. **公式碎片散落正文**：把第 2 条去掉后，纯字形碎片（`z` / `ph` / `V =`）不再成图，反而炸成几十个单字块（UsingColor 段落中位长 145 → 65）。

**修复**：
- 表格：重建后比对「区域内的原始字符数」与「重建单元格字符数」，覆盖不足 80% 就**保留原始文本**并写入告警；
- 公式：**判定照旧**（保证方程成图），但**裁图矩形剔除非公式行**，且**正文行永不并入图片** —— 既不丢字，方程也仍是图；
- 含简单算式的正文不再被改判为公式（`R2 = 0.72`、`× 4 CPU` 这类句子的段落此前会被换字体且不参与翻译）。

### 问题 9：第 7 页表格少了最后一行

**根因**：FSI 那一行的左格**折行**（左格占两条基线，右格缩写夹在中间），2.5pt 的基线分组把三条线拆成三个单行组，全部被丢弃，
最后一行于是以正文形式落在表格外面。

**修复**：新增 `_absorb_table_tail()`：在表格下方一个行距范围内，把仍使用同一列结构的行吸收为表格行；若该范围内出现任何不属于该结构的行，
则放弃扩展（绝不让表格吞掉正文）。实测该表 **17 行 → 18 行**（含 FSI）。

### 问题 10：加粗/斜体在网页里丢失

**修复**：按 span 提取行内 `(text, bold, italic)` run（字体名 + flags 位双重判定），写入 IR 新增的 `emphasis` 字段与 `blocks.emphasis` 列，
前端 `renderEmphasis()` 渲染为 `<strong>` / `<em>`，并与搜索高亮叠加。实测该论文 **88/173 块**带样式 run。

### 问题 11（前端三项）

1. **目录沉在整份文献库之后** → 改为**紧跟当前文献卡片下方**（限高 42vh 可滚动）；
2. **切换双语/译文/原文后阅读位置丢失** → 滚动时记录「视口顶部所在段落」，切换后用 `useLayoutEffect` 把该段重新钉到顶部（模式切换会改变每块高度，浏览器钳制 scrollTop 造成「跳回开头」）；
3. **界面语言** → 新增 `frontend/src/i18n/`（中文原文为 key，按区域拆字典），**默认跟随系统语言**（中文系统→中文，其余→英文），顶栏 `中 / EN` 可手动切换并持久化；共 313 个界面字符串，键无缺失。

### 第三轮结果（同一批 9 篇）

| 论文 | 覆盖率 | 续接断裂 | 页末残句 | 中位段长 |
| --- | --- | --- | --- | --- |
| 3DPhenoMVS | 0.985 → 0.984 | 0 → **0** | 0 → **0** | 389 → **473** |
| applsci | 0.962 → 0.963 | 5 → 6 | 0 → **0** | 258 → 261 |
| Comprehensive | 0.963 → **0.997** | 29 → 25 | 5 → 12 | 360 → 427 |
| Field-grown | 0.949 → **0.971** | 18 → 17 | 2 → 2 | 100 → 108 |
| fpls | 0.943 → **0.960** | 22 → 18 | 2 → 5 | 94 → **181** |
| Leaf Area | 0.985 → **0.993** | 5 → 7 | 1 → 6 | 186 → 218 |
| plants | 0.964 → 0.967 | 9 → **7** | 2 → 2 | 211 → 302 |
| sustainability | 0.994 → 0.994 | 4 → **0** | 2 → **0** | 289 → 333 |
| Using color | **0.874 → 0.964** | 41 → **22** | 4 → 6 | 137 → 136 |

覆盖率（= 文本有没有丢）全部持平或明显上升；`test_parser.py` 与 `smoke.py` 全部通过，前端 `tsc --noEmit`、`npm run build` 通过。

---

=== 主 Prompt 开始 ===

## 一、你的角色与目标

你是资深全栈工程师 + 文档解析专家。请在现有 Essay Reader 仓库上**增量改进**，重点解决四类问题：**分段碎裂、目录污染、浮层遮挡、通道启用引导缺失**。不要重写架构，不要引入需要联网的运行时依赖。

## 二、分段与目录的正确性（最高优先级）

### 2.1 段落合并必须"文档自适应"，禁止行级猜标题

- 段落合并只能依赖**几何与样式证据**（行起点间距分布、字号变化、栏归属、字体粗细），**不得**用"这一行以数字开头"之类的文本规则来决定断段。
- 行距阈值必须从本栏/本页的行距分布中学习（例如行起点间距的一维二分），并设一道下限保护：阈值不得低于该文档的行距中位数 × 1.3，否则均匀行距的正文会被切成一行一段。
- 允许同行框重叠（部分出版商的 PDF 行框会互相覆盖，gap 为负），此时以**行起点间距**而非行间 gap 计算。
- 需要为"单栏但第一行有缩进"的页面做正确判断：缩进不应被当成栏归属或分段依据。

### 2.2 标题判定必须可解释、可回归

维护一个**单一入口**的标题判定函数（如 `_looks_like_section_heading`），规则至少包含：

1. 节号必须是**小整数**（1–2 位）、最多 3 级（`3.1.2`），且节号后紧跟的必须是**字母**开头的标题文本；
2. 明确否决测量值与数值区间：`0.72 to 0.97`、`0.51 cm, 0.42 cm, 0.98 cm`；
3. 明确否决元数据/日期：`29 June 2022) and http://…`；
4. 明确否决跑头与页码：`5 of 17`、`Page 5 of 17`、`5/17`、单独的数字；
5. 明确否决图注（`Figure 2.` / `Table 1:`）与过短符号（`∑`）；
6. 字面规则命中后，**还要**满足最小字母数（≥ 4）与非符号字符数（≥ 6）。

`_line_is_standalone()` 只允许以下情况触发独立成段：页码/跑头、图注开头、真标题、短冒号结尾行。**绝不允许**因为"以数字开头"就断段。

### 2.3 目录（大纲）的产出要求

- 目录只允许来自 `heading` 类型块，且必须**排除**跑头、页码、图注、参考文献条目、页脚；
- 第 1 页顶部的**跨行标题必须合并为一个 L1**：连续、间距小于约 2.5 倍行高、字号相近的标题行合成一条，续行打 `title-continuation` 标记并从目录移除；
- 期刊品牌/页眉色带上的浅色文字（span 颜色接近白色）不得进入目录，标 `faint-text` 并转为 `header`；
- 输出层级要求：论文主标题 L1，一级章节 L2（`1. Introduction`），二级 L3（`2.1.`），最多 3 级；
- 校验：目录中不得出现任何匹配 `^\d+\s*(of|/)\s*\d+$` 或 `^0\.\d` 的条目。

### 2.4 必须补充的自动化测试（本次已加入，请保持并通过）

用**真实论文**做回归样本（仓库已有 `backend/tests/samples/real_world_3dphenomvs.pdf`，17 页单栏 MDPI 风格），断言：

- 第 5 页块数 ≤ 12，正文段落中位长度 ≥ 60 字符；
- 跨块句子完整（例如 `conducted from two different viewpoints` 与 `at the top. Then, it was placed` 同时存在）；
- 目录里没有 `N of M`、没有 `0.x` 开头的条目；
- 目录包含 `1. Introduction`、`2. Materials and Methods`、`3. Results`；
- 全篇文本覆盖率 ≥ 99%（重新分块不得丢字）；
- 块 ID 全局唯一、每页都有块、`order` 与索引一致；
- 合成样本（双栏带图表表格 / 单栏 / 扫描件 OCR）三组测试继续通过。

## 三、浮层与层叠上下文（前端硬性要求）

- **任何**下拉、弹层、浮窗都不得依赖父级 `z-index` 数值来解决遮挡。`backdrop-filter`、`transform`、`filter`、`will-change` 都会创建层叠上下文，使子元素无法越过兄弟层。
- 统一使用 `FloatingPanel`（`createPortal` → `document.body` + `position: fixed` + 实测锚点坐标），并处理：
  - 视口边界（左右不出屏、上下翻转）；
  - `resize` 与滚动（含捕获阶段）时重新定位；
  - 点击外部与 `Esc` 关闭；
  - 无障碍：`role="dialog"`、焦点管理。
- 移动端（窄视口）上面板宽度自适应，不出现横向溢出。

## 四、通道不可用时的引导与"一键部署"（产品要求）

### 4.1 诊断接口

`GET /api/setup/guide` 必须返回：

- 本地：可用性、详细原因、`needs` 枚举（`ready` / `install_runtime` / `pull_model`）、是否检测到 Ollama 可执行文件及其路径、已安装模型列表、端点、当前模型名；
- 云端：可用性、详细原因、`base_url`、`model`、是否已配置 Key（**不得回显 Key**）；
- `links`：Ollama 下载页、安装包直链、模型库、OpenAI 兼容文档、DeepSeek Key 申请页；
- `commands`：Windows 静默安装、Linux 安装脚本、`ollama pull <model>`。

### 4.2 一键动作

- `POST /api/models/pull`：应用内下载模型（SSE 进度），下载完成后自动重新检测；
- `POST /api/setup/download-ollama`：SSE 流式下载官方安装包到 `data/downloads/`，返回进度与落盘路径；
  - **只下载，不静默安装**；安装动作必须由用户在自己的机器上显式执行，界面同时给出 `OllamaSetup.exe /SILENT` 与官网链接；
  - 仅允许 `https://` 且限制扩展名；
  - 无外网环境要给出"离线导入模型目录（`%USERPROFILE%\.ollama\models`）"的说明。

### 4.3 引导界面要求

- 按**依赖顺序**分步（装运行时 → 启动服务 → 下载模型 → 切换通道），每步有状态徽标（已完成/待完成/等待前置）与对应动作；
- 每步都要给出**官方链接 + 等价命令行**，不能只有"请自行安装"；
- 云端路径给出 Base URL / API Key 表单、获取 Key 的链接、保存后立即复检；
- 引导必须可从顶栏通道菜单随时重新打开（不可用通道显示「查看启用步骤」）；主按钮在通道不可用时变为「启用通道」并打开引导；
- 明确告知：解析/阅读/搜索/笔记/图表**不需要**任何模型通道，只有翻译、要点、问答需要。

## 五、验收标准

1. 上述 2.4 的全部断言通过（`python backend/tests/test_parser.py` 退出码 0，且 `smoke.py` 也通过）；
2. 用真实 PDF 手动验证：第 5 页段落完整、目录只含真实章节、无跑头/测量值；
3. 顶栏两个下拉（通道、导出）在阅读区长文档下**不被遮挡**，滚动与缩放窗口后位置正确；
4. 在未安装 Ollama 的机器上：通道菜单 →「查看启用步骤」能完成"下载安装包 → 下载模型 → 切换通道"的引导闭环，且每一步都有明确的成功/失败反馈；
5. 断网时：引导页与所有本地功能照常工作，云端按钮置灰并给出原因；
6. 不引入新的联网运行时依赖（OCR 与解析保持离线）。

## 六、输出要求

按以下顺序给出：① 每个问题的根因说明与证据（页码/块号/原文片段）；② 代码改动清单（文件 + 函数 + 为什么这样改）；③ 回归测试结果；④ 仍然存在的已知限制与下一步建议。

如果你认为我的某项要求会伤害通用性（例如某种启发式在别的出版商会误判），请直接指出并给出更稳健的替代方案，而不是照做。

=== 主 Prompt 结束 ===

---

## 附：如何验证 AI 链路（翻译 / 要点 / 问答）

本轮**无法验证模型输出质量**——执行环境里既没有 Ollama，也没有云端 API Key（`/api/setup/guide` 已确认：`local.needs = install_runtime`、`cloud.api_key_set = false`）。因此已验证的是**代码路径与错误处理**，不是译文质量。

### 已验证的部分（不需要模型）

| 项 | 结果 |
| --- | --- |
| 翻译预估 | 275 段 → 26.6k / 16k token → 云端 ≈ ¥0.059，本地 ≈ 581 秒 |
| 通道不可用时的失败路径 | 本地/云端各自在数秒内返回可执行错误，不再重试风暴 |
| 术语表、占位符保护、质量校验 | 单元测试覆盖（`smoke.py` 全绿） |
| 任务队列与进度上报 | `/api/tasks/{id}` 正常推进与终止 |

### 你需要做的（任选一条即可打通）

**A. 本地模型（免费、离线，推荐）**

1. 打开应用 → 顶栏通道菜单 →「查看启用步骤」；
2. 点「一键下载安装包」（会存到 `backend/data/downloads/OllamaSetup.exe`），或去 `https://ollama.com/download` 下载；
3. 双击安装（静默安装：`OllamaSetup.exe /SILENT`）；
4. 回到引导页 → 点「重新检测」→「一键下载模型」（默认 `qwen3:8b`，约 4~5 GB）；
5. 切到「本地模型」→ 上传一篇 PDF → 点「翻译全文」。

**B. 云端模型（更快更准，按量计费）**

1. 通道菜单 →「查看启用步骤」→ 切到云端页签；
2. 填 Base URL（默认 `https://api.deepseek.com/v1`）与 API Key（[申请地址](https://platform.deepseek.com/api_keys)）；
3. 点「保存并检测」→ 切到「云端模型」→ 翻译全文。

### 打通后请重点看这几件事

1. **译文是否漏译/多译**：随机抽 5 段，用「双语」模式与原文对照；
2. **数字与引用是否保真**：看正文左侧有没有黄色「待复核」标记，有则点「用云端重译此段」；
3. **术语一致性**：在「术语表」里看自动抽取的术语，同一个词是否全文一致；
4. **要点总结**：点「要点」→「生成结构化要点」，检查是否出现原文没有的内容（幻觉）；
5. **问答带出处**：点「问答」提问，看回答是否给出章节/页码，以及「文中未提及」是否正确触发；
6. **本地 vs 云端对比**：同一段落用两种通道各翻一次，缓存会分别保存，可直接对比。

### 如果你想让我（AI）来验证

我需要下面任一项，之后可以自己跑通全链路并逐项给出证据：

- **本机装好 Ollama 并拉一个模型**（我可以直接调用 `http://127.0.0.1:11434` 验证）；或
- **一个可用的云端 API Key**（写进 `backend/.env` 或告诉我，我用 `/api/settings` 配置后跑端到端）；或
- **允许我下载安装包**（`/api/setup/download-ollama` 已实现）——但安装动作需要你在本机确认，我无法代你点击安装程序。

> 安全提示：如果提供云端 Key，建议用一个**限额的测试 Key**，验证完可随时在服务端吊销。
